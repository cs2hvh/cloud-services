# E2E Test Environment Variables

# Test user credentials (update with actual test account)
TEST_USER_EMAIL=deep.aghera@ahurasense.com
TEST_USER_PASSWORD=Ahura.c0.co1

# Admin user credentials (update with actual admin account)
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=admin-password

# Base URL for tests
BASE_URL=http://localhost:3000

# Supabase (for integration tests)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key

# Feature flags
ENABLE_E2E_MOCKS=true
