name: data-runner image

# Builds and pushes the ahura-data-runner Node image to GHCR on every
# push to a tracked branch that touches data-runner or runner-core source.
# Mirrors eval-runner-image.yml — the data-runner is the same shape of
# deployable (Node + runner-core, no GPU), built from the shared ./workers
# context so the file: dependency on runner-core resolves.
on:
  push:
    branches: [ai, master, dev, "feature/ai-phase-5-ai-agent-work-3.0", "feature/s3-doc-fetch-ai-pahse-6"]
    paths:
      - "workers/data-runner/**"
      - "workers/runner-core/**"
      - ".github/workflows/data-runner-image.yml"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/ahura-data-runner

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/master' }}

      - name: Build + push
        uses: docker/build-push-action@v6
        with:
          context: ./workers
          file: ./workers/data-runner/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=data-runner
          cache-to: type=gha,mode=max,scope=data-runner

      - name: Summary
        run: |
          echo "### Pushed image" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
