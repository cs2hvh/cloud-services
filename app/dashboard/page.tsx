import { SidebarLayout } from "@/components/dashboard/sidebar/layout";

const DashboardPage = async () => {
    // const users = await query.users.get_all();
    // console.log(users);
    return (
        <SidebarLayout >
            <div>
                asd
            </div>
        </SidebarLayout>
    )
}

export default DashboardPage